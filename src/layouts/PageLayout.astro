---
import Layout from "./Layout.astro";
import { resolvePageSchema } from "../utils/schema";

const fm = (Astro.props as any).frontmatter ?? {};

const title = Astro.props.title ?? fm.title;
const description = Astro.props.description ?? fm.description;
const metaTitle = Astro.props.metaTitle ?? fm.metaTitle;
const publishDate = Astro.props.publishDate ?? fm.publishDate;
const lastModified = Astro.props.lastModified ?? fm.lastModified;
const author = Astro.props.author ?? fm.author;
const schemaType = Astro.props.schemaType ?? fm.schemaType ?? null;

// ── Schema ────────────────────────────────────────────────────────────────────
// "about" is handled inline here (it's complex enough to warrant its own block).
// Everything else is resolved via the shared utility.

const siteUrl = "https://dailyaimail.com";
const logoUrl = `${siteUrl}/logo.png`;
const pubIso = (publishDate ?? "2026-01-01").replace(/\//g, "-");
const modIso = (lastModified ?? "2026-03-15").replace(/\//g, "-");

let pageSchema: object | null = null;

if (schemaType === "about") {
  // About Us — the one page that fully redefines Organization + Person
  pageSchema = {
    "@context": "https://schema.org",
    "@graph": [
      {
        "@type": "AboutPage",
        "@id": `${siteUrl}/about-us`,
        url: `${siteUrl}/about-us`,
        name:
          metaTitle ??
          "About Daily AI Mail — Independent AI News, Research & Intelligence",
        headline: "About Daily AI Mail",
        description:
          description ??
          "Daily AI Mail is an independent publication covering artificial intelligence news.",
        inLanguage: "en-US",
        isPartOf: { "@id": `${siteUrl}/#website` },
        mainEntity: { "@id": `${siteUrl}/#organization` },
        about: { "@id": `${siteUrl}/#organization` },
        author: { "@id": `${siteUrl}/#/schema/person/mohamed-diab` },
        publisher: { "@id": `${siteUrl}/#organization` },
        datePublished: `${pubIso}T00:00:00+00:00`,
        dateModified: `${modIso}T00:00:00+00:00`,
        breadcrumb: { "@id": `${siteUrl}/about-us#breadcrumb` },
        isAccessibleForFree: true,
        speakable: {
          "@type": "SpeakableSpecification",
          cssSelector: [".page-title"],
        },
        relatedLink: [
          `${siteUrl}/publishing-principles`,
          `${siteUrl}/contact-us`,
        ],
        mentions: [
          { "@id": `${siteUrl}/#/schema/person/mohamed-diab` },
          { "@id": `${siteUrl}/#organization` },
        ],
      },
      {
        "@type": "BreadcrumbList",
        "@id": `${siteUrl}/about-us#breadcrumb`,
        itemListElement: [
          {
            "@type": "ListItem",
            position: 1,
            name: "Home",
            item: `${siteUrl}/`,
          },
          {
            "@type": "ListItem",
            position: 2,
            name: "About Us",
            item: `${siteUrl}/about-us`,
          },
        ],
      },
      {
        "@type": "NewsMediaOrganization",
        "@id": `${siteUrl}/#organization`,
        name: "Daily AI Mail",
        url: `${siteUrl}/`,
        foundingDate: "2026-01-01",
        description:
          "Daily AI Mail is an independent digital publication covering the global artificial intelligence industry.",
        publishingPrinciples: `${siteUrl}/publishing-principles`,
        ethicsPolicy: `${siteUrl}/publishing-principles`,
        masthead: `${siteUrl}/about-us`,
        correctionsPolicy: `${siteUrl}/publishing-principles`,
        missionCoveragePrioritiesPolicy: `${siteUrl}/publishing-principles`,
        logo: {
          "@type": "ImageObject",
          "@id": `${siteUrl}/#/schema/logo/image/`,
          url: logoUrl,
          contentUrl: logoUrl,
          width: 512,
          height: 512,
          caption: "Daily AI Mail",
        },
        image: { "@id": `${siteUrl}/#/schema/logo/image/` },
        member: { "@id": `${siteUrl}/#/schema/person/mohamed-diab` },
        founder: { "@id": `${siteUrl}/#/schema/person/mohamed-diab` },
        subjectOf: { "@id": `${siteUrl}/about-us` },
        contactPoint: [
          {
            "@type": "ContactPoint",
            email: "editorial@dailyaimail.com",
            contactType: "editorial",
          },
          {
            "@type": "ContactPoint",
            email: "press@dailyaimail.com",
            contactType: "press",
          },
          {
            "@type": "ContactPoint",
            email: "corrections@dailyaimail.com",
            contactType: "corrections",
          },
        ],
        knowsAbout: [
          "Artificial Intelligence",
          "Machine Learning",
          "Large Language Models",
          "AI Safety",
          "Generative AI",
          "AI Policy",
        ],
        sameAs: [
          "https://twitter.com/dailyaimail",
          "https://linkedin.com/company/dailyaimail",
          "https://diab.digital",
        ],
      },
      {
        "@type": "Person",
        "@id": `${siteUrl}/#/schema/person/mohamed-diab`,
        name: "Mohamed Diab",
        givenName: "Mohamed",
        familyName: "Diab",
        url: "https://interactiveseo.digital",
        jobTitle: "Founder & Editor-in-Chief",
        description:
          "Technical SEO Consultant and founder of Daily AI Mail, with over ten years of experience in the technology and digital marketing industry.",
        worksFor: { "@id": `${siteUrl}/#organization` },
        knowsAbout: [
          "Technical SEO",
          "Structured Data",
          "Knowledge Graph Optimization",
          "Artificial Intelligence",
          "Digital Publishing",
          "Entity-Based SEO",
        ],
        homeLocation: { "@type": "Place", name: "Egypt" },
        sameAs: ["https://interactiveseo.digital", "https://diab.digital"],
      },
      {
        "@type": "WebSite",
        "@id": `${siteUrl}/#website`,
        url: `${siteUrl}/`,
        name: "Daily AI Mail",
        publisher: { "@id": `${siteUrl}/#organization` },
      },
    ],
  };
} else {
  // All other page types resolved via the shared utility
  pageSchema = resolvePageSchema(schemaType, {
    title: title ?? "",
    description: description ?? "",
    publishDate: pubIso,
    lastModified: modIso,
    metaTitle,
  });
}
---

<Layout title={title} description={description} metaTitle={metaTitle}>
  {
    pageSchema && (
      <script
        type="application/ld+json"
        set:html={JSON.stringify(pageSchema)}
        slot="head"
      />
    )
  }

  <main class="page-main">
    <div class="page-container">
      <header class="page-header">
        <h1 class="page-title">{title}</h1>
        <div class="page-meta">
          {author && <span>By {author}</span>}
          {publishDate && <span>Published {publishDate}</span>}
          {lastModified && <span>Last updated {lastModified}</span>}
        </div>
      </header>
      <div class="page-content">
        <slot />
      </div>
    </div>
  </main>
</Layout>

<style>
  .page-main {
    min-height: 100vh;
    padding: 80px var(--gutter) var(--gutter);
  }
  .page-container {
    max-width: 720px;
    margin: 0 auto;
  }
  .page-header {
    padding-bottom: 32px;
    border-bottom: 1px solid var(--color-border);
    margin-bottom: 40px;
  }
  .page-title {
    font-family: var(--font-display);
    font-size: clamp(32px, 5vw, 52px);
    line-height: 1.1;
    margin-bottom: 16px;
  }
  .page-meta {
    display: flex;
    flex-wrap: wrap;
    gap: 16px;
    font-size: 14px;
    color: var(--color-text-muted);
  }
  .page-content {
    font-size: 16px;
    line-height: 1.75;
    color: var(--color-text-muted);
  }
  .page-content h2 {
    font-family: var(--font-display);
    font-size: 24px;
    color: var(--color-text);
    margin: 40px 0 16px;
  }
  .page-content h3 {
    font-family: var(--font-display);
    font-size: 19px;
    color: var(--color-text);
    margin: 28px 0 12px;
  }
  .page-content p {
    margin-bottom: 16px;
  }
  .page-content a {
    text-decoration: underline;
  }
  .page-content table {
    width: 100%;
    border-collapse: collapse;
    margin: 24px 0;
    font-size: 14px;
  }
  .page-content th {
    text-align: left;
    padding: 10px 14px;
    border-bottom: 2px solid var(--color-border);
    color: var(--color-text);
    font-weight: 600;
  }
  .page-content td {
    padding: 10px 14px;
    border-bottom: 1px solid var(--color-border);
  }
  .page-content ul,
  .page-content ol {
    margin: 0 0 16px 24px;
  }
  .page-content li {
    margin-bottom: 6px;
  }
  .page-content strong {
    color: var(--color-text);
    font-weight: 600;
  }
  .page-content hr {
    border: none;
    border-top: 1px solid var(--color-border);
    margin: 40px 0;
  }
</style>
